<ul class="nav nav-tabs" role="tablist">
  <%= content_tag_for(:tr, @lists) do |list| %>
    <li class="active"><%= link_to "#{list.name}", list %></li>
  <% end %>
  <li>  <%= link_to new_list_path, :class => 'btn btn-primary' do %>
      <span class="glyphicon glyphicon-plus"></span>
      New List
    <% end %>
  </li>
</ul>

<td><%# link_to 'Edit', edit_list_path(list) %></td>
<td><%# link_to 'Destroy', list, method: :delete, data: { confirm: 'Are you sure?' } %></td>



<div class="page-header">
  <%= link_to lists_path, :class => 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <%= link_to edit_list_path(@list), :class => 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-pencil"></span>
    Edit
  <% end %>
  <h1>Show list</h1>
</div>

<dl class="dl-horizontal">
  <dt>Name:</dt>
  <dd><%= @list.name %></dd>

  <dt>User:</dt>
  <dd><%= @list.user_id %></dd>

</dl>
